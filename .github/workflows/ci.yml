name: CI

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]

jobs:
  placeholder:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Echo start
        run: echo "Starting CI pipeline for node-js-application-fortesting"

      - name: Echo node version placeholder
        run: echo "Node setup and install steps will go here"

      - name: Echo test placeholder
        run: echo "Test steps will go here"

      - name: Echo variables
        run: |
          echo "Username: ${{ vars.USERNAME }}"
          echo "Password: ${{ vars.PASSWORD }}"

      - name: Echo done
        run: echo "CI pipeline placeholder complete"
