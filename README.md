# node-js-application

A simple Node.js HTTP server.

## Run

```
npm start
```

Server listens on `http://localhost:3000` (`/` returns a greeting, `/health` returns status JSON).

## Test

```
npm test
```

## CI

`.github/workflows/ci.yml` currently contains placeholder echo steps only.
